# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env with secrets (VITE_SUPABASE_*). .env.local is already covered by *.local.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session infra (preview config + local permissions)
.claude/

# Android signing keys — NEVER commit these. They live in GitHub Secrets (base64)
# and on your machine only. A leaked keystore lets anyone ship updates as this app.
*.jks
*.keystore
